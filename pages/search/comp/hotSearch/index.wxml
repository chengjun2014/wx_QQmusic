<view>
  <text bindtap='search' data-key="{{special.key}}" class="hot-key special">{{special.key}}</text>
  <block wx:for="{{hotSearch}}" wx:key="unique">
    <text bindtap='search' data-key="{{item.k}}" class="hot-key" wx:if="{{index < 10}}">{{item.k}}</text>
  </block>
</view>
