<!--pages/playing/index.wxml-->
<map id="map" longitude="{{lng}}" latitude="{{lat}}" scale="{{scale}}" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location></map>
